
NUM_TRIALS = 2;
result_Ts = zeros(2, NUM_TRIALS);
result_thetas = zeros(22, NUM_TRIALS);

for j=1:NUM_TRIALS
    evalc('main_play_and_collect_data_and_computer_play;');
    result_Ts()Ts
    best_theta
    clearvars best_theta exp_feat feat_exp;
    
end